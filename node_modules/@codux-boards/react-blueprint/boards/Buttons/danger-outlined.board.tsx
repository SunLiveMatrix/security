import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/danger-outlined.svg';

export default createBoard({
    name: 'Danger outlined',
    Board: () => (
        <Button large={true} intent="danger" outlined={true}>
            Danger
        </Button>
    ),
    tags: ['button', 'danger', 'outlined'],
    cover,
});
