import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/primary-outlined.svg';

export default createBoard({
    name: 'Primary outlined',
    Board: () => (
        <Button large={true} intent="primary" outlined={true}>
            Primary
        </Button>
    ),
    tags: ['button', 'primary', 'outlined'],
    cover,
});
