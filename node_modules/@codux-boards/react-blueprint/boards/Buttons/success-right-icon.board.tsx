import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/success-right-icon.svg';

export default createBoard({
    name: 'Success right icon',
    Board: () => (
        <Button rightIcon="arrow-right" large={true} intent="success">
            Next step{' '}
        </Button>
    ),
    tags: ['button', 'success', 'arrow', 'icon'],
    cover,
});
