import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/primary-small.svg';

export default createBoard({
    name: 'Primary small',
    Board: () => (
        <Button large={false} intent="primary">
            Primary
        </Button>
    ),
    tags: ['button', 'primary', 'small'],
    cover,
});
