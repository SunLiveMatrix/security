import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/none-outlined.svg';

export default createBoard({
    name: 'None outlined',
    Board: () => (
        <Button
            large={true}
            intent="none"
            minimal={true}
            icon="document"
            rightIcon="cross"
        >
            Upload...{' '}
        </Button>
    ),
    tags: ['button', 'none', 'outlined', 'icon', 'upload', 'file'],
    cover,
});
