import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/warning.svg';

export default createBoard({
    name: 'Warning',
    Board: () => (
        <Button large={true} intent="warning">
            Warning
        </Button>
    ),
    tags: ['button', 'warning'],
    cover,
});
