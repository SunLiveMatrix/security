import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/primary-minimal.svg';

export default createBoard({
    name: 'Primary minimal',
    Board: () => (
        <Button large={true} intent="primary" minimal icon={'clean'}>
            Clean Cash
        </Button>
    ),
    tags: ['button', 'primary', 'minimal', 'icon', 'clean'],
    cover,
});
