import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/none-left-icon.svg';

export default createBoard({
    name: 'None left icon',
    Board: () => (
        <Button icon="tick" large={true} intent="none">
            Submit
        </Button>
    ),
    tags: ['button', 'none', 'tick', 'icon'],
    cover,
});
