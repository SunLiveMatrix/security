import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/none-small.svg';

export default createBoard({
    name: 'None small',
    Board: () => (
        <Button large={false} intent="none">
            None
        </Button>
    ),
    tags: ['button', 'none', 'small'],
    cover,
});
