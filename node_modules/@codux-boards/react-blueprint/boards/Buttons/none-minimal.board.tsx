import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/none-minimal.svg';

export default createBoard({
    name: 'None minimal',
    Board: () => (
        <Button large={true} intent="none" minimal={true}>
            Text button{' '}
        </Button>
    ),
    tags: ['button', 'none', 'minimal', 'text'],
    cover,
});
