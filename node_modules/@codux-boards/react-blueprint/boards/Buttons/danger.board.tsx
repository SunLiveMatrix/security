import { createBoard } from '@wixc3/react-board';
import { Button } from '@blueprintjs/core';
// board cover
import cover from '../../../../assets/buttons/danger.svg';

export default createBoard({
    name: 'Danger',
    Board: () => (
        <Button large={true} intent="danger">
            Danger
        </Button>
    ),
    tags: ['button', 'danger'],
    cover,
});
