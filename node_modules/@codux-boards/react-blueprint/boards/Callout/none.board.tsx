import { createBoard } from '@wixc3/react-board';
import { Callout, Code } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/callout/none.svg';

export default createBoard({
    name: 'None',
    Board: () => {
        return (
            <Callout title="Callout Title">
                Callout text body, <a>text link</a>, <Code>code</Code>, etc.
            </Callout>
        );
    },
    tags: ['callout', 'none'],
    cover,
});
