import { createBoard } from '@wixc3/react-board';
import { Callout } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/callout/warning.svg';

export default createBoard({
    name: 'Warning',
    Board: () => {
        return <Callout intent="warning">Callout text body</Callout>;
    },
    tags: ['callout', 'warning'],
    cover,
});
