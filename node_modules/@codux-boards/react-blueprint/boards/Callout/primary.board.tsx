import { createBoard } from '@wixc3/react-board';
import { Callout } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/callout/primary.svg';

export default createBoard({
    name: 'Primary',
    Board: () => {
        return <Callout intent="primary">Callout text body</Callout>;
    },
    tags: ['callout', 'primary'],
    cover,
});
