import { createBoard } from '@wixc3/react-board';
import { Callout } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/callout/danger.svg';

export default createBoard({
    name: 'Danger',
    Board: () => {
        return <Callout intent="danger">Callout text body</Callout>;
    },
    tags: ['callout', 'danger'],
    cover,
});
