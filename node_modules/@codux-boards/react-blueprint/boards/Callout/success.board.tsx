import { createBoard } from '@wixc3/react-board';
import { Callout } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/callout/success.svg';

export default createBoard({
    name: 'Success',
    Board: () => {
        return <Callout intent="success">Callout text body</Callout>;
    },
    tags: ['callout', 'success'],
    cover,
});
