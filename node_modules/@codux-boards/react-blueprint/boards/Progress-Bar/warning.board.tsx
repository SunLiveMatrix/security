import { createBoard } from '@wixc3/react-board';
import { ProgressBar } from '@blueprintjs/core';

// board cover
import cover from '../../../../../src/assets/progress-bar/warning.svg';

export default createBoard({
    name: 'Warning',
    Board: () => {
        return (
            <div style={{ width: '240px' }}>
                <ProgressBar intent="warning" value={0.3} />
            </div>
        );
    },
    tags: ['progress', 'bar', 'warning'],
    cover,
    environmentProps: {
        canvasWidth: 240,
    },
});
