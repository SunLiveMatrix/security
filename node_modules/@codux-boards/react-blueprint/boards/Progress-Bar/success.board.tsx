import { createBoard } from '@wixc3/react-board';
import { ProgressBar } from '@blueprintjs/core';

// board cover
import cover from '../../../../../src/assets/progress-bar/success.svg';

export default createBoard({
    name: 'Success',
    Board: () => {
        return (
            <div style={{ width: '240px' }}>
                <ProgressBar intent="success" value={0.5} />
            </div>
        );
    },
    tags: ['progress', 'bar', 'success'],
    cover,
    environmentProps: {
        canvasWidth: 240,
    },
});
