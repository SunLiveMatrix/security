import { createBoard } from '@wixc3/react-board';
import { ProgressBar } from '@blueprintjs/core';

// board cover
import cover from '../../../../../src/assets/progress-bar/danger.svg';

export default createBoard({
    name: 'Danger',
    Board: () => {
        return (
            <div style={{ width: '240px' }}>
                <ProgressBar intent="danger" value={0.6} />
            </div>
        );
    },
    tags: ['progress', 'bar', 'danger'],
    cover,
    environmentProps: {
        canvasWidth: 240,
    },
});
