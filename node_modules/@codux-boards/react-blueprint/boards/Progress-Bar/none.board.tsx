import { createBoard } from '@wixc3/react-board';
import { ProgressBar } from '@blueprintjs/core';

// board cover
import cover from '../../../../../src/assets/progress-bar/none.svg';

export default createBoard({
    name: 'None',
    Board: () => {
        return (
            <div style={{ width: '240px' }}>
                <ProgressBar intent="none" value={0.3} />
            </div>
        );
    },
    tags: ['progress', 'bar', 'none'],
    cover,
});
