import { createBoard } from '@wixc3/react-board';
import { InputGroup } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/form-inputs/text-input.svg';

export default createBoard({
    name: 'Text input',
    Board: () => (
        <InputGroup placeholder="Enter your username..." leftIcon="user" />
    ),
    tags: ['input', 'text', 'user'],
    cover,
});
