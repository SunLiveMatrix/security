import { createBoard } from '@wixc3/react-board';
import { Classes, Tooltip } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/tooltip/visual-intent.svg';

export default createBoard({
    name: 'Tooltip - visual intent',
    Board: () => {
        return (
            <>
                Available in the full range of{' '}
                <Tooltip
                    className={Classes.TOOLTIP_INDICATOR}
                    content="Success"
                    intent="success"
                    position="right"
                >
                    visual intents!
                </Tooltip>
            </>
        );
    },
    tags: ['intent', 'tooltip'],
    cover,
});
