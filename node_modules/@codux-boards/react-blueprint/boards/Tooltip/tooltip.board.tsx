import { createBoard } from '@wixc3/react-board';
import { AnchorButton, Tooltip } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/tooltip/tooltip.svg';

export default createBoard({
    name: 'Tooltip',
    Board: () => {
        return (
            <Tooltip content={'Tooltip'}>
                <AnchorButton style={{ textDecoration: 'none' }} intent="none">
                    Hover to see me
                </AnchorButton>
            </Tooltip>
        );
    },
    tags: ['tooltip'],
    cover,
});
