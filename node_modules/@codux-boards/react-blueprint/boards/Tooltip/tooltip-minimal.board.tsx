import { createBoard } from '@wixc3/react-board';
import { Classes, Tooltip } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/tooltip/minimal-tooltip.svg';

export default createBoard({
    name: 'Tooltip - minimal',
    Board: () => {
        return (
            <>
                This line's{' '}
                <Tooltip
                    className={Classes.TOOLTIP_INDICATOR}
                    content={
                        <span>
                            Tooltip on <strong>bottom</strong>
                        </span>
                    }
                    minimal
                    position="bottom"
                >
                    tooltip
                </Tooltip>{' '}
                is minimal
            </>
        );
    },
    tags: ['minimal', 'tooltip'],
    cover,
});
