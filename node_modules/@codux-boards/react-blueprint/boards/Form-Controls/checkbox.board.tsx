import { createBoard } from '@wixc3/react-board';
import { Checkbox, Label } from '@blueprintjs/core';

// board cover
import cover from '../../../../assets/form-controls/checkbox.svg';

export default createBoard({
    name: 'Checkbox',
    Board: () => {
        return (
            <>
                <Label>Assign responsibility</Label>
                <Checkbox label="Gilad Gray" checked />
                <Checkbox label="Jason Killian" />
                <Checkbox label="Antoine Llorca" disabled />
            </>
        );
    },
    tags: ['checkbox'],
    cover,
});
